import React from 'react';

const Clients = ({ clients, loading}) => {
    if(loading) {
        return <h>Loading....</h>
    }

    return (
        <div>
         
        </div>
    )
}

export default Clients
